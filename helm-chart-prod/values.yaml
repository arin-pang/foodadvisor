# Default values for referenceapp.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

#replicaCount: 1

namespace: food-advisor

## Pass the required profile maps to the spring profile environment variable
global:
  # profile: dev
  pullPolicy: Always
  # probe:
  #   liveness:
  #     path: /api/health
  #     initialDelay: 150
  #     intervalPeriod: 3
  #     failureThreshold: 10
  #   readiness:
  #     path: /api/health
  #     initialDelay: 150

deployment:
  image:
    repo: registry.ondot-kr.gq/food-advisor

# service:
#   type: ClusterIP
  # port: 3000
  # nodePort: 443

nameOverride: ""
fullnameOverride: ""


resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
# requests:
#  cpu: 100m
#  memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
